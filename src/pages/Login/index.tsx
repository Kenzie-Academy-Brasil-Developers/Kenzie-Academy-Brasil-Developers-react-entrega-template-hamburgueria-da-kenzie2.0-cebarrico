import { LoginForm } from "./LoginForm";
import { InfoSection } from "../../components/InfoSection";

export const LoginPage = () => {
  return (
    <main>
      <InfoSection />
      <LoginForm />
    </main>
  );
};
