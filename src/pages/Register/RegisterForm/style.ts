import styled from "styled-components";

export const SectionFormStyle = styled.section`
  div {
    width: 90%;
    max-width: 500px;

    display: flex;
    justify-content: space-between;
    align-items: center;

    padding: 0 25px;
  }

  a {
    font-weight: 500;
    font-size: 14px;
    color: var(--color-grey-2);

    cursor: pointer;
  }
`;
